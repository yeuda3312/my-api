services:
  - type: web
    name: yemot-grok-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server_grok_only:app
    envVars:
      - key: GROK_API_KEY
        sync: false
